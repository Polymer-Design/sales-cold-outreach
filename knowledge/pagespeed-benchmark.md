---
status: PENDING
---

# Polymer client PageSpeed benchmark

**PENDING - not citable yet.** Real mobile PageSpeed/Lighthouse scores are being pulled
from shipped Polymer client sites (in progress, per the ICP docs). Until this file's
status flips to `approved` with real averaged numbers filled in below, no email may make a
**comparative** PageSpeed claim ("you score X, our sites average Y").

A lead's OWN PageSpeed score is still fine to cite as a plain fact (see
`scripts/pagespeed_check.py` and the lead-scoring skill) - that needs no benchmark. This
file only gates the comparative version of the claim.

## Once populated, this section holds:

- Sample size (how many shipped Polymer sites)
- Average mobile performance score
- Average mobile load time / speed index
- Date last refreshed
