# [ALARM] Daily pipeline blocked (day 2): Apollo API plan still lacks search access

Today's daily-run (2026-08-04) hit the same blocker as yesterday: mixed_people/search
still returns 403 - "not included in your Free plan... Upgrade your plan from
https://www.apollo.io/pricing". Confirmed again with:

  python scripts/apollo_client.py people-search --icp startups --page 1

email_accounts still works fine (ethan@integratedchurchwebsites.com is connected), so the
API key itself is valid - this is specifically a plan-tier restriction on the search
endpoint both ICPs' ingestion step depends on.

No new leads were ingested this run. Church ingestion had nothing to do anyway
(data/leads/churches/candidates.csv still doesn't exist - church-crawler hasn't produced
candidates yet). data/leads/index.jsonl is still empty/missing, so scoring, drafting, and
pushing had nothing to process. Queue directories (drafts/, pending-approval/,
approved-to-send/) are all empty too.

This is now two consecutive days blocked on the same root cause. Action needed: upgrade
the Apollo plan to one with API search access, then re-run the daily pipeline.
