# [ALARM] Daily pipeline blocked again: Apollo API plan still lacks search access

Today's daily-run (2026-08-19) hit the same blocker seen on every run since 2026-08-03 (03, 04, 06, 07, 10, 11, 12, 13, 14, 17, 18). `mixed_people/search` still returns 403 - "not included in your Free plan... Upgrade your plan from https://www.apollo.io/pricing". Confirmed again with:

  python scripts/apollo_client.py people-search --icp startups --page 1

This is a plan-tier restriction, not a bad key or expired token - both ICPs' ingestion step depend on this endpoint (churches ingestion also calls people search per org domain, once candidates.csv has rows).

No new leads were ingested this run. Church ingestion had nothing to do anyway - data/leads/churches/candidates.csv still doesn't exist (church-crawler hasn't produced candidates yet). data/leads/index.jsonl is still empty/missing, so scoring, drafting, and pushing had nothing to process. Queue directories (drafts/, pending-approval/, approved-to-send/, sent/) remain empty. Recycle check had nothing to check either.

This is now the twelfth known blocked run on the same root cause. GitHub issue #3 already tracks this; adding an update there instead of opening a duplicate. No action possible on this end until the Apollo plan is upgraded.

Mailbox reconciliation note unchanged from prior runs: `email-accounts` shows only ethan@integratedchurchwebsites.com connected. config/domains.yaml still lists all four mailboxes as REPLACE_ME. Not blocking anything today since mode is dry_run.
