# [ALARM] Daily pipeline blocked again: Apollo API plan still lacks search access

Today's daily-run (2026-08-11) hit the same blocker seen on 2026-08-03, 08-04, 08-06, 08-07, and 08-10: mixed_people/search still returns 403 - "not included in your Free plan... Upgrade your plan from https://www.apollo.io/pricing". Confirmed again with:

  python scripts/apollo_client.py people-search --icp startups --page 1

This is a plan-tier restriction, not a bad key or expired token - both ICPs' ingestion step depend on this endpoint (churches ingestion also calls people search per org domain, once candidates.csv has rows).

No new leads were ingested this run. Church ingestion had nothing to do anyway - data/leads/churches/candidates.csv still doesn't exist (church-crawler hasn't produced candidates yet, or its weekly run hasn't landed any). data/leads/index.jsonl is still empty/missing, so scoring, drafting, and pushing had nothing to process. Queue directories (drafts/, pending-approval/, approved-to-send/, sent/) remain empty.

This is now the sixth known blocked run on the same root cause (2026-08-03, 08-04, 08-06, 08-07, 08-10, 08-11). GitHub issue #3 already tracks this; adding an update there instead of opening a duplicate. No action possible on this end until the Apollo plan is upgraded.
