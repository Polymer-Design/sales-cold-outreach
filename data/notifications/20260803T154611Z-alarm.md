# [ALARM] Daily pipeline blocked: Apollo API plan lacks search access

Today's daily-run (2026-08-03) could not ingest any startup leads: `mixed_people/search`
returned 403 - "not included in your Free plan... Upgrade your plan from
https://www.apollo.io/pricing". Confirmed with:

  python scripts/apollo_client.py people-search --icp startups --page 1

email_accounts and emailer_campaigns/search (used for stats) both return fine, so the API
key itself is valid - this is specifically a plan-tier restriction on the search endpoint
both ICPs' ingestion step depends on (churches ingestion also calls people search per org
domain, once candidates.csv has rows).

No new leads were ingested this run as a result. Church ingestion had nothing to do anyway
(data/leads/churches/candidates.csv doesn't exist yet - church-crawler hasn't produced
candidates). data/leads/index.jsonl is still empty, so scoring/drafting/pushing had nothing
to process either.

Action needed: upgrade the Apollo plan to one that includes API search access (any paid
plan, per Apollo's error message), then re-run the daily pipeline.
