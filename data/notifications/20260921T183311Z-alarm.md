# [ALARM] Daily pipeline blocked: Apollo plan lacks People Search API access

Today's daily run (2026-09-21) could not ingest any new leads.

scripts/apollo_client.py people-search (POST mixed_people/search) returned:
403 API_INACCESSIBLE - "The api/v1/mixed_people/search API is not included in your
Free plan and is not accessible, even with a master key. All paid plans include full
API access."

Impact:
- Startups ICP: cannot run Apollo people search at all - step 1 ingestion is fully blocked.
- Churches ICP: no candidates.csv rows with status:new today, so no impact yet, but the
  per-org decision-maker lookup (Apollo people search on organization domain) will hit
  the same 403 as soon as the crawler produces candidates.

Other Apollo endpoints (email-accounts, stats) work fine with the current key, so this is
specifically the People Search product being excluded from the current (Free) Apollo plan,
not a general auth/key problem.

Action needed: upgrade the Apollo plan to one that includes full API access (any paid
plan per Apollo's error message), then re-run the daily pipeline.

No leads, scores, drafts, or Apollo writes happened this run as a result. data/leads/index.jsonl
remains empty/nonexistent.
