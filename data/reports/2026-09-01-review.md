# Outreach review 2026-09-01

## TLDR

Nothing changed in the two weeks since the last review: the system is still pre-launch,
`config/system.yaml` is still `dry_run`, and `scripts/preflight.py --skip-apollo` fails on the
exact same two blockers as 2026-08-15. There is still no send/reply performance to review —
zero leads scored, zero drafts, zero replies. Ethan's one action is unchanged: connect the 4
warmed mailboxes to Apollo and fill in `config/domains.yaml`, then drop in a first approved
case study, so the daily pipeline has something to produce before the next review.

## Numbers vs. last review

| Metric | This review (2026-09-01) | Last review (2026-08-15) |
|---|---|---|
| Sends | 0 | 0 |
| Delivery rate | N/A | N/A |
| Open rate | N/A | N/A |
| Reply rate | N/A | N/A |
| Positive-reply rate | N/A | N/A |
| Booked calls | 0 | 0 |

`scripts/apollo_client.py stats` returns zero sequences (same as last review).
`data/replies/log.jsonl` doesn't exist yet — `data/replies/.last-check` shows the reply-check
job has been running on schedule (last run 2026-09-01T17:09:09Z, "no new replies"), which is
the expected no-op with no mailboxes connected.

## Deliverability

No mailboxes are connected to Apollo. `config/domains.yaml` still has both `startups` mailbox
slots and both `churches` mailbox slots set to `REPLACE_ME`. No TrulyInbox health, bounce, or
opt-out data exists to check. No alarm — nothing is sending.

## Objection themes

None — no reply log exists yet.

## Experiments

Nothing running, correctly. `data/experiments.md` is unchanged from last review: only the
backlog of proposed tests, zero sends to justify starting one. The skill's own gate (100
sends/arm minimum) still isn't met by a wide margin. First experiment (subject-style: short
noun phrase vs. question form) should open once the first sequence is actually live.

## Changes made autonomously

None. `config/playbook.yaml` stays at `version: 1` — there is still no performance data to
justify any auto-applied copy change.

## Recommendations needing sign-off

These are the same four items flagged on 2026-08-15; none have been actioned yet, so they
carry forward rather than being re-derived from scratch:

1. **Connect the 4 warmed mailboxes** (2 on `launchwithpolymer.com`, 2 on
   `integratedchurchwebsites.com`) to Apollo via OAuth and fill in `config/domains.yaml`.
   Still the current live-mode blocker per `scripts/preflight.py --skip-apollo`.
2. **Add at least one approved case study** to `knowledge/case-studies/` (one per ICP
   ideally). Still only `README.md` and `_template.md` in that directory. Preflight allows
   launch without one but drafts will keep skipping proof entirely until it exists.
3. Once mailboxes are connected, **run the daily pipeline manually once** to generate the
   first real drafts, then review those for template smell/voice drift before flipping
   `mode: live`.
4. **Doc drift still unfixed**: `CLAUDE.md` still describes `config/icp-*.yaml` as
   "still `status: draft` - treat filters as provisional," but both `icp-startups.yaml` and
   `icp-churches.yaml` have been `status: final` since 2026-07-13. Small edit, worth doing so
   a future run doesn't misread the ICP filters as provisional.
