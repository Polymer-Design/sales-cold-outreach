# Outreach review 2026-09-15

## TLDR

No change since the last two reviews: the system is still pre-launch, `config/system.yaml`
is still `dry_run`, and `scripts/preflight.py --skip-apollo` fails on the same two blockers
as 2026-08-15 and 2026-09-01. There is still zero send/reply performance to review. Ethan's
one action is unchanged: connect the 4 warmed mailboxes to Apollo and fill in
`config/domains.yaml`, then drop in a first approved case study, so the daily pipeline has
something to produce before the next review.

## Numbers vs. last review

| Metric | This review (2026-09-15) | Last review (2026-09-01) |
|---|---|---|
| Sends | 0 | 0 |
| Delivery rate | N/A | N/A |
| Open rate | N/A | N/A |
| Reply rate | N/A | N/A |
| Positive-reply rate | N/A | N/A |
| Booked calls | 0 | 0 |

`scripts/apollo_client.py stats` returns zero sequences (same as last review).
`data/replies/log.jsonl` still doesn't exist — `data/replies/.last-check` shows the
reply-check job is still running on schedule, which is the expected no-op with no mailboxes
connected. `data/queue/` is empty in every state (drafts, pending-approval, approved-to-send,
sent) — zero leads have been drafted.

## Deliverability

No mailboxes are connected to Apollo. `config/domains.yaml` still has both `startups`
mailbox slots and both `churches` mailbox slots set to `REPLACE_ME`. No TrulyInbox health,
bounce, or opt-out data exists to check. No alarm — nothing is sending.

## Objection themes

None — no reply log exists yet.

## Experiments

Nothing running, correctly. `data/experiments.md` is unchanged from last review: only the
backlog of proposed tests, zero sends to justify starting one. The skill's own gate (100
sends/arm minimum) still isn't met by a wide margin. First experiment (subject-style: short
noun phrase vs. question form) should open once the first sequence is actually live.

## Changes made autonomously

None. `config/playbook.yaml` stays at `version: 1` — there is still no performance data to
justify any auto-applied copy change.

## Recommendations needing sign-off

These are the same items flagged on 2026-08-15 and 2026-09-01; none have been actioned yet,
so they carry forward rather than being re-derived from scratch:

1. **Connect the 4 warmed mailboxes** (2 on `launchwithpolymer.com`, 2 on
   `integratedchurchwebsites.com`) to Apollo via OAuth and fill in `config/domains.yaml`.
   Still the current live-mode blocker per `scripts/preflight.py --skip-apollo`.
2. **Add at least one approved case study** to `knowledge/case-studies/` (one per ICP
   ideally). Still only `README.md` and `_template.md` in that directory. Preflight allows
   launch without one but drafts will keep skipping proof entirely until it exists.
3. Once mailboxes are connected, **run the daily pipeline manually once** to generate the
   first real drafts, then review those for template smell/voice drift before flipping
   `mode: live`.
4. **Doc drift still unfixed**: `CLAUDE.md` still describes `config/icp-*.yaml` as
   "still `status: draft` - treat filters as provisional," but both `icp-startups.yaml` and
   `icp-churches.yaml` have been `status: final` since 2026-07-13. This is now the third
   consecutive review flagging this one-line fix; it hasn't caused a misread yet only because
   nothing has consumed the ICP filters for a real draft yet, but it should be fixed before
   the pipeline goes live and starts reading them under time pressure.
