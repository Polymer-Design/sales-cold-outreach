# Outreach review 2026-08-15

## TLDR

The system is still pre-launch: `config/system.yaml` is in `dry_run`, zero leads have been
scored, zero drafts sit in `data/queue/`, and Apollo shows no sequences — so there is no
send/reply performance to review this cycle. The only things blocking a first live run are
the two mailbox pairs still marked `REPLACE_ME` in `config/domains.yaml` and the absence of
an approved case study in `knowledge/case-studies/`. Ethan's one action: connect the 4 warmed
mailboxes to Apollo via OAuth (and drop in a first approved case study) so the daily pipeline
can produce real drafts before the next review.

## Numbers vs. last review

No prior review exists (this is the first biweekly run). No sequence, mailbox, or reply data
exists yet — `scripts/apollo_client.py stats` returns zero sequences and `data/replies/log.jsonl`
is empty.

| Metric | This review | Last review |
|---|---|---|
| Sends | 0 | N/A (first review) |
| Delivery rate | N/A | N/A |
| Open rate | N/A | N/A |
| Reply rate | N/A | N/A |
| Positive-reply rate | N/A | N/A |
| Booked calls | 0 | N/A |

## Deliverability

No mailboxes are connected to Apollo yet (`config/domains.yaml` startups/churches mailboxes
are still `REPLACE_ME`), so there is no TrulyInbox health, bounce, or opt-out data to check.
No alarm — there is nothing sending yet to alarm on.

## Objection themes

None — `data/replies/log.jsonl` has no entries.

## Experiments

Nothing running. `data/experiments.md` still shows the original backlog only, which is
correct at this stage: the skill's own rule is minimum 100 sends/arm before judging anything,
and there have been zero sends. Starting a split test now would just be guessing. First
experiment (recommend the subject-style test from the backlog: short noun phrase vs.
question form) should open once the first real sequence is live and producing volume.

## Changes made autonomously

None. `config/playbook.yaml` stays at `version: 1` (initial defaults, no test has produced a
winner yet) — there is no performance data to justify any auto-applied change.

## Recommendations needing sign-off

1. **Connect the 4 warmed mailboxes** (2 on `launchwithpolymer.com`, 2 on
   `integratedchurchwebsites.com`) to Apollo via OAuth and fill in `config/domains.yaml`.
   Confirmed via `scripts/preflight.py --skip-apollo`: this is the current live-mode blocker.
2. **Add at least one approved case study** to `knowledge/case-studies/` (one per ICP ideally).
   Preflight flags this too — launch is technically possible without one, but drafts will
   have to lean on the business overview only and skip proof entirely until it exists.
3. Once mailboxes are connected, **run the daily pipeline manually once** (README setup step
   7) to generate the first real drafts, then review those for template smell/voice drift
   before flipping `mode: live`.
4. Minor doc drift: `CLAUDE.md` still describes `config/icp-*.yaml` as "still `status: draft`
   - treat filters as provisional," but both `icp-startups.yaml` and `icp-churches.yaml` were
   locked to `status: final` on 2026-07-13. Worth a quick edit so future runs don't misread the
   ICP filters as provisional.
