# Call prep: Fix Verification Test — Polymer QA
**When:** 2026-08-02T15:00:00-04:00          **ICP:** startups          **Track:** new_build_track
**Lead score:** Not in our outreach system — booked directly

## What we sent them
n/a — no match found in `data/leads/index.jsonl` or `data/queue/`.

## What we found on them
No background found. This booking's email (`test@example.com`) and org name ("Polymer QA")
don't correspond to a known lead or a real prospect organization — `test@example.com` uses
the IANA-reserved `example.com` domain, and the booker name is "Fix Verification Test." This
looks like a synthetic/QA entry exercising the call-prep pipeline rather than a real prospect
call. Flagging this plainly rather than presenting it as a real lead.

## Their current stack
- Platform: unknown (fresh check against https://example.com — no platform markers detected)
- PageSpeed (mobile, https://example.com): performance 100, accessibility 96, best practices 96,
  SEO 80, largest contentful paint 0.8s

## Reply history
Booked directly, no outreach history.

## Suggested angle for the call
No real call to prep for — if this shows up on the calendar as an actual appointment, verify
with Ethan directly before treating it as a live prospect conversation.
